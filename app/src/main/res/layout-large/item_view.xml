<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/item_view"
    android:layout_width="match_parent"
    android:layout_height="@dimen/tablet_item_height"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/item_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/tablet_horizontal_margin"
        android:layout_marginEnd="@dimen/tablet_horizontal_margin"
        android:scaleType="fitXY"
        app:srcCompat="@drawable/ic_list_icon" />

    <TextView
        android:id="@+id/country_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="5"
        android:textColor="@color/colorPrimaryText"
        android:textSize="@dimen/large_item_text_size" />

</LinearLayout>